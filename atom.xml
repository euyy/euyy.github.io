<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://euyy.github.io</id>
    <title>Gridea</title>
    <updated>2023-02-11T03:27:06.658Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://euyy.github.io"/>
    <link rel="self" href="https://euyy.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://euyy.github.io/images/avatar.png</logo>
    <icon>https://euyy.github.io/favicon.ico</icon>
    <rights>All rights reserved 2023, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[Git使用教程]]></title>
        <id>https://euyy.github.io/post/git-shi-yong-jiao-cheng/</id>
        <link href="https://euyy.github.io/post/git-shi-yong-jiao-cheng/">
        </link>
        <updated>2023-02-10T14:29:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="git-安装配置">Git 安装配置</h1>
<h2 id="git-配置">Git 配置</h2>
<p>Git 提供了一个叫做 git config 的工具，专门用来配置或读取相应的工作环境变量。</p>
<p>这些环境变量，决定了 Git 在各个环节的具体工作方式和行为。这些变量可以存放在以下三个不同的地方：</p>
<ul>
<li><code>/etc/gitconfig</code> 文件：系统中对所有用户都普遍适用的配置。若使用 <code>git config</code> 时用 <code>--system</code> 选项，读写的就是这个文件。</li>
<li><code>~/.gitconfig</code> 文件：用户目录下的配置文件只适用于该用户。若使用 <code>git config</code> 时用 <code>--global</code> 选项，读写的就是这个文件。</li>
<li>当前项目的 Git 目录中的配置文件（也就是工作目录中的 <code>.git/config</code> 文件）：这里的配置仅仅针对当前项目有效。每一个级别的配置都会覆盖上层的相同配置，所以 <code>.git/config</code> 里的配置会覆盖 <code>/etc/gitconfig</code> 中的同名变量。</li>
</ul>
<p>在 Windows 系统上，Git 会找寻用户主目录下的 .gitconfig 文件。主目录即 $HOME 变量指定的目录，一般都是 C:\Documents and Settings$USER。</p>
<p>此外，Git 还会尝试找寻 /etc/gitconfig 文件，只不过看当初 Git 装在什么目录，就以此作为根目录来定位。</p>
<h3 id="用户信息">用户信息</h3>
<p>配置个人的用户名称和电子邮件地址：</p>
<pre><code class="language-bash">$ git config --global user.name &quot;runoob&quot;
$ git config --global user.email test@runoob.com
</code></pre>
<p>如果用了 <strong>--global</strong> 选项，那么更改的配置文件就是位于你用户主目录下的那个，以后你所有的项目都会默认使用这里配置的用户信息。</p>
<p>如果要在某个特定的项目中使用其他名字或者电邮，只要去掉 --global 选项重新配置即可，新的设定保存在当前项目的 .git/config 文件里。</p>
<h3 id="文本编辑器">文本编辑器</h3>
<p>设置Git默认使用的文本编辑器, 一般可能会是 Vi 或者 Vim。如果你有其他偏好，比如 Emacs 的话，可以重新设置：:</p>
<pre><code class="language-bash">$ git config --global core.editor emacs
</code></pre>
<h3 id="差异分析工具">差异分析工具</h3>
<p>还有一个比较常用的是，在解决合并冲突时使用哪种差异分析工具。比如要改用 vimdiff 的话：</p>
<pre><code class="language-bash">$ git config --global merge.tool vimdiff
</code></pre>
<p>Git 可以理解 kdiff3，tkdiff，meld，xxdiff，emerge，vimdiff，gvimdiff，ecmerge，和 opendiff 等合并工具的输出信息。</p>
<p>当然，你也可以指定使用自己开发的工具，具体怎么做可以参阅第七章。</p>
<h3 id="查看配置信息">查看配置信息</h3>
<p>要检查已有的配置信息，可以使用 git config --list 命令：</p>
<pre><code class="language-bash">$ git config --list
http.postbuffer=2M
user.name=runoob
user.email=test@runoob.com
</code></pre>
<p>有时候会看到重复的变量名，那就说明它们来自不同的配置文件（比如 /etc/gitconfig 和 ~/.gitconfig），不过最终 Git 实际采用的是最后一个。</p>
<p>这些配置我们也可以在 <strong>~/.gitconfig</strong> 或 <strong>/etc/gitconfig</strong> 看到，如下所示：</p>
<pre><code>vim ~/.gitconfig 
</code></pre>
<p>显示内容如下所示：</p>
<pre><code>[http]
    postBuffer = 2M
[user]
    name = runoob
    email = test@runoob.com
</code></pre>
<p>也可以直接查阅某个环境变量的设定，只要把特定的名字跟在后面即可，像这样：</p>
<pre><code class="language-bash">$ git config user.name
runoob
</code></pre>
<h1 id="git-工作区-暂存区和版本库">Git 工作区、暂存区和版本库</h1>
<hr>
<h2 id="基本概念">基本概念</h2>
<p>我们先来理解下 Git 工作区、暂存区和版本库概念：</p>
<ul>
<li>**工作区：**就是你在电脑里能看到的目录。</li>
<li>**暂存区：**英文叫 stage 或 index。一般存放在 <strong>.git</strong> 目录下的 index 文件（.git/index）中，所以我们把暂存区有时也叫作索引（index）。</li>
<li>**版本库：**工作区有一个隐藏目录 <strong>.git</strong>，这个不算工作区，而是 Git 的版本库。</li>
</ul>
<p>下面这个图展示了工作区、版本库中的暂存区和版本库之间的关系：</p>
<figure data-type="image" tabindex="1"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/1352126739_7909.jpg" alt="img" loading="lazy"></figure>
<ul>
<li>图中左侧为工作区，右侧为版本库。在版本库中标记为 &quot;index&quot; 的区域是暂存区（stage/index），标记为 &quot;master&quot; 的是 master 分支所代表的目录树。</li>
<li>图中我们可以看出此时 &quot;HEAD&quot; 实际是指向 master 分支的一个&quot;游标&quot;。所以图示的命令中出现 HEAD 的地方可以用 master 来替换。</li>
<li>图中的 objects 标识的区域为 Git 的对象库，实际位于 &quot;.git/objects&quot; 目录下，里面包含了创建的各种对象及内容。</li>
<li>当对工作区修改（或新增）的文件执行 <strong>git add</strong> 命令时，暂存区的目录树被更新，同时工作区修改（或新增）的文件内容被写入到对象库中的一个新的对象中，而该对象的ID被记录在暂存区的文件索引中。</li>
<li>当执行提交操作（git commit）时，暂存区的目录树写到版本库（对象库）中，master 分支会做相应的更新。即 master 指向的目录树就是提交时暂存区的目录树。</li>
<li>当执行 <strong>git reset HEAD</strong> 命令时，暂存区的目录树会被重写，被 master 分支指向的目录树所替换，但是工作区不受影响。</li>
<li>当执行 <strong>git rm --cached <file></strong> 命令时，会直接从暂存区删除文件，工作区则不做出改变。</li>
<li>当执行 <strong>git checkout .</strong> 或者 <strong>git checkout -- <file></strong> 命令时，会用暂存区全部或指定的文件替换工作区的文件。这个操作很危险，会清除工作区中未添加到暂存区中的改动。</li>
<li>当执行 <strong>git checkout HEAD .</strong> 或者 <strong>git checkout HEAD <file></strong> 命令时，会用 HEAD 指向的 master 分支中的全部或者部分文件替换暂存区和以及工作区中的文件。这个命令也是极具危险性的，因为不但会清除工作区中未提交的改动，也会清除暂存区中未提交的改动。</li>
</ul>
<h1 id="git-创建仓库">Git 创建仓库</h1>
<h2 id="git-init">git init</h2>
<p>Git 使用 <strong>git init</strong> 命令来初始化一个 Git 仓库，Git 的很多命令都需要在 Git 的仓库中运行，所以 <strong>git init</strong> 是使用 Git 的第一个命令。</p>
<p>在执行完成 <strong>git init</strong> 命令后，Git 仓库会生成一个 .git 目录，该目录包含了资源的所有元数据，其他的项目目录保持不变。</p>
<h3 id="使用方法">使用方法</h3>
<p>使用当前目录作为 Git 仓库，我们只需使它初始化。</p>
<pre><code>git init
</code></pre>
<p>该命令执行完后会在当前目录生成一个 .git 目录。</p>
<p>使用我们指定目录作为Git仓库。</p>
<pre><code>git init newrepo
</code></pre>
<p>初始化会在 newrepo 目录下会出现一个名为 .git 的目录，所有 Git 需要的数据和资源都存放在这个目录中。</p>
<p>如果当前目录下有几个文件想要纳入版本控制，需要先用 git add 命令告诉 Git 开始对这些文件进行跟踪，然后提交：</p>
<pre><code class="language-bash">$ git add *.c
$ git add README
$ git commit -m '初始化项目版本'
</code></pre>
<p>以上命令将目录下以 .c 结尾及 README 文件提交到仓库中。</p>
<blockquote>
<p><strong>注：</strong> 在 Linux 系统中，commit 信息使用单引号 <strong>'</strong>，Windows 系统，commit 信息使用双引号 <strong>&quot;</strong>。</p>
<p>所以在 git bash 中 <strong>git commit -m '提交说明'</strong> 这样是可以的，在 Windows 命令行中就要使用双引号 <strong>git commit -m &quot;提交说明&quot;</strong>。</p>
</blockquote>
<h2 id="git-clone">git clone</h2>
<p>我们使用 <strong>git clone</strong> 从现有 Git 仓库中拷贝项目（类似 <strong>svn checkout</strong>）。</p>
<p>克隆仓库的命令格式为：</p>
<pre><code>git clone &lt;repo&gt;
</code></pre>
<p>如果我们需要克隆到指定的目录，可以使用以下命令格式：</p>
<pre><code>git clone &lt;repo&gt; &lt;directory&gt;
</code></pre>
<p><strong>参数说明：</strong></p>
<ul>
<li>**repo:**Git 仓库。</li>
<li>**directory:**本地目录。</li>
</ul>
<p>比如，要克隆 Ruby 语言的 Git 代码仓库 Grit，可以用下面的命令：</p>
<pre><code class="language-bash">$ git clone git://github.com/schacon/grit.git
</code></pre>
<p>执行该命令后，会在当前目录下创建一个名为grit的目录，其中包含一个 .git 的目录，用于保存下载下来的所有版本记录。</p>
<p>如果要自己定义要新建的项目目录名称，可以在上面的命令末尾指定新的名字：</p>
<pre><code class="language-bash">$ git clone git://github.com/schacon/grit.git mygrit
</code></pre>
<h2 id="配置">配置</h2>
<p>git 的设置使用 <strong>git config</strong> 命令。</p>
<p>显示当前的 git 配置信息：</p>
<pre><code class="language-bash">$ git config --list
credential.helper=osxkeychain
core.repositoryformatversion=0
core.filemode=true
core.bare=false
core.logallrefupdates=true
core.ignorecase=true
core.precomposeunicode=true
</code></pre>
<p>编辑 git 配置文件:</p>
<pre><code class="language-bash">$ git config -e    # 针对当前仓库 
</code></pre>
<p>或者：</p>
<pre><code class="language-bash">$ git config -e --global   # 针对系统上所有仓库
</code></pre>
<p>设置提交代码时的用户信息：</p>
<pre><code class="language-bash">$ git config --global user.name &quot;runoob&quot;
$ git config --global user.email test@runoob.com
</code></pre>
<p>如果去掉 <strong>--global</strong> 参数只对当前仓库有效。</p>
<h1 id="git-基本操作">Git 基本操作</h1>
<p>Git 的工作就是创建和保存你项目的快照及与之后的快照进行对比。</p>
<p>本章将对有关创建与提交你的项目快照的命令作介绍。</p>
<p>Git 常用的是以下 6 个命令：<strong>git clone</strong>、<strong>git push</strong>、<strong>git add</strong> 、<strong>git commit</strong>、<strong>git checkout</strong>、<strong>git pull</strong>，后面我们会详细介绍。</p>
<figure data-type="image" tabindex="2"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/git-command.jpg" alt="img" loading="lazy"></figure>
<p><strong>说明：</strong></p>
<ul>
<li>workspace：工作区</li>
<li>staging area：暂存区/缓存区</li>
<li>local repository：版本库或本地仓库</li>
<li>remote repository：远程仓库</li>
</ul>
<p>一个简单的操作步骤：</p>
<pre><code class="language-bash">$ git init    
$ git add .    
$ git commit  
</code></pre>
<ul>
<li>git init - 初始化仓库。</li>
<li>git add . - 添加文件到暂存区。</li>
<li>git commit - 将暂存区内容添加到仓库中。</li>
</ul>
<h3 id="创建仓库命令">创建仓库命令</h3>
<p>下表列出了 git 创建仓库的命令：</p>
<table>
<thead>
<tr>
<th style="text-align:left">命令</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>git init</code></td>
<td style="text-align:left">初始化仓库</td>
</tr>
<tr>
<td style="text-align:left"><code>git clone</code></td>
<td style="text-align:left">拷贝一份远程仓库，也就是下载一个项目。</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="提交与修改">提交与修改</h2>
<p>Git 的工作就是创建和保存你的项目的快照及与之后的快照进行对比。</p>
<p>下表列出了有关创建与提交你的项目的快照的命令：</p>
<table>
<thead>
<tr>
<th style="text-align:left">命令</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>git add</code></td>
<td style="text-align:left">添加文件到暂存区</td>
</tr>
<tr>
<td style="text-align:left"><code>git status</code></td>
<td style="text-align:left">查看仓库当前的状态，显示有变更的文件。</td>
</tr>
<tr>
<td style="text-align:left"><code>git diff</code></td>
<td style="text-align:left">比较文件的不同，即暂存区和工作区的差异。</td>
</tr>
<tr>
<td style="text-align:left"><code>git commit</code></td>
<td style="text-align:left">提交暂存区到本地仓库。</td>
</tr>
<tr>
<td style="text-align:left"><code>git reset</code></td>
<td style="text-align:left">回退版本。</td>
</tr>
<tr>
<td style="text-align:left"><code>git rm</code></td>
<td style="text-align:left">将文件从暂存区和工作区中删除。</td>
</tr>
<tr>
<td style="text-align:left"><code>git mv</code></td>
<td style="text-align:left">移动或重命名工作区文件。</td>
</tr>
</tbody>
</table>
<h3 id="提交日志">提交日志</h3>
<table>
<thead>
<tr>
<th style="text-align:left">命令</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>git log</code></td>
<td style="text-align:left">查看历史提交记录</td>
</tr>
<tr>
<td style="text-align:left"><code>git blame &lt;file&gt;</code></td>
<td style="text-align:left">以列表形式查看指定文件的历史修改记录</td>
</tr>
</tbody>
</table>
<h3 id="远程操作">远程操作</h3>
<table>
<thead>
<tr>
<th style="text-align:left">命令</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>git remote</code></td>
<td style="text-align:left">远程仓库操作</td>
</tr>
<tr>
<td style="text-align:left"><code>git fetch</code></td>
<td style="text-align:left">从远程获取代码库</td>
</tr>
<tr>
<td style="text-align:left"><code>git pull</code></td>
<td style="text-align:left">下载远程代码并合并</td>
</tr>
<tr>
<td style="text-align:left"><code>git push</code></td>
<td style="text-align:left">上传远程代码并合并</td>
</tr>
</tbody>
</table>
<h1 id="git-分支管理">Git 分支管理</h1>
<p>几乎每一种版本控制系统都以某种形式支持分支，一个分支代表一条独立的开发线。</p>
<p>使用分支意味着你可以从开发主线上分离开来，然后在不影响主线的同时继续工作。</p>
<figure data-type="image" tabindex="3"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/git-brance.svg" alt="img" loading="lazy"></figure>
<p>Git 分支实际上是指向更改快照的指针。</p>
<p>有人把 Git 的分支模型称为<strong>必杀技特性</strong>，而正是因为它，将 <strong>Git</strong> 从版本控制系统家族里区分出来。</p>
<p>创建分支命令：</p>
<pre><code>git branch (branchname)
</code></pre>
<p>切换分支命令:</p>
<pre><code>git checkout (branchname)
</code></pre>
<p>当你切换分支的时候，Git 会用该分支的最后提交的快照替换你的工作目录的内容， 所以多个分支不需要多个目录。</p>
<p>合并分支命令:</p>
<pre><code>git merge 
</code></pre>
<p>你可以多次合并到统一分支， 也可以选择在合并之后直接删除被并入的分支。</p>
<p>开始前我们先创建一个测试目录：</p>
<pre><code class="language-bash">$ mkdir gitdemo
$ cd gitdemo/
$ git init
Initialized empty Git repository...
$ touch README
$ git add README
$ git commit -m '第一次版本提交'
[master (root-commit) 3b58100] 第一次版本提交
 1 file changed, 0 insertions(+), 0 deletions(-)
 create mode 100644 README
</code></pre>
<hr>
<h2 id="git-分支管理-2">Git 分支管理</h2>
<h3 id="列出分支">列出分支</h3>
<p>列出分支基本命令：</p>
<pre><code>git branch
</code></pre>
<p>没有参数时，<strong>git branch</strong> 会列出你在本地的分支。</p>
<pre><code class="language-bash">$ git branch
* master
</code></pre>
<p>此例的意思就是，我们有一个叫做 <strong>master</strong> 的分支，并且该分支是当前分支。</p>
<p>当你执行 <strong>git init</strong> 的时候，默认情况下 Git 就会为你创建 <strong>master</strong> 分支。</p>
<p>如果我们要手动创建一个分支。执行 <strong>git branch (branchname)</strong> 即可。</p>
<pre><code class="language-bash">$ git branch testing
$ git branch
* master
  testing
</code></pre>
<p>现在我们可以看到，有了一个新分支 <strong>testing</strong>。</p>
<p>当你以此方式在上次提交更新之后创建了新分支，如果后来又有更新提交， 然后又切换到了 <strong>testing</strong> 分支，Git 将还原你的工作目录到你创建分支时候的样子。</p>
<p>接下来我们将演示如何切换分支，我们用 git checkout (branch) 切换到我们要修改的分支。</p>
<pre><code class="language-bash">$ ls
README
$ echo 'runoob.com' &gt; test.txt
$ git add .
$ git commit -m 'add test.txt'
[master 3e92c19] add test.txt
 1 file changed, 1 insertion(+)
 create mode 100644 test.txt
$ ls
README        test.txt
$ git checkout testing
Switched to branch 'testing'
$ ls
README
</code></pre>
<p>当我们切换到 <strong>testing</strong> 分支的时候，我们添加的新文件 test.txt 被移除了。切换回 <strong>master</strong> 分支的时候，它们又重新出现了。</p>
<pre><code class="language-bash">$ git checkout master
Switched to branch 'master'
$ ls
README        test.txt
</code></pre>
<p>我们也可以使用 git checkout -b (branchname) 命令来创建新分支并立即切换到该分支下，从而在该分支中操作。</p>
<pre><code class="language-bash">$ git checkout -b newtest
Switched to a new branch 'newtest'
$ git rm test.txt 
rm 'test.txt'
$ ls
README
$ touch runoob.php
$ git add .
$ git commit -am 'removed test.txt、add runoob.php'
[newtest c1501a2] removed test.txt、add runoob.php
 2 files changed, 1 deletion(-)
 create mode 100644 runoob.php
 delete mode 100644 test.txt
$ ls
README        runoob.php
$ git checkout master
Switched to branch 'master'
$ ls
README        test.txt
</code></pre>
<p>如你所见，我们创建了一个分支，在该分支上移除了一些文件 test.txt，并添加了 runoob.php 文件，然后切换回我们的主分支，删除的 test.txt 文件又回来了，且新增加的 runoob.php 不存在主分支中。</p>
<p>使用分支将工作切分开来，从而让我们能够在不同开发环境中做事，并来回切换。</p>
<h3 id="删除分支">删除分支</h3>
<p>删除分支命令：</p>
<pre><code>git branch -d (branchname)
</code></pre>
<p>例如我们要删除 testing 分支：</p>
<pre><code class="language-bash">$ git branch
* master
  testing
$ git branch -d testing
Deleted branch testing (was 85fc7e7).
$ git branch
* master
</code></pre>
<h3 id="分支合并">分支合并</h3>
<p>一旦某分支有了独立内容，你终究会希望将它合并回到你的主分支。 你可以使用以下命令将任何分支合并到当前分支中去：</p>
<pre><code>git merge
$ git branch
* master
  newtest
$ ls
README        test.txt
$ git merge newtest
Updating 3e92c19..c1501a2
Fast-forward
 runoob.php | 0
 test.txt   | 1 -
 2 files changed, 1 deletion(-)
 create mode 100644 runoob.php
 delete mode 100644 test.txt
$ ls
README        runoob.php
</code></pre>
<p>以上实例中我们将 newtest 分支合并到主分支去，test.txt 文件被删除。</p>
<p>合并完后就可以删除分支:</p>
<pre><code class="language-bash">$ git branch -d newtest
Deleted branch newtest (was c1501a2).
</code></pre>
<p>删除后， 就只剩下 master 分支了：</p>
<pre><code class="language-bash">$ git branch
* master
</code></pre>
<h3 id="合并冲突">合并冲突</h3>
<p>合并并不仅仅是简单的文件添加、移除的操作，Git 也会合并修改。</p>
<pre><code class="language-bash">$ git branch
* master
$ cat runoob.php
</code></pre>
<p>首先，我们创建一个叫做 change_site 的分支，切换过去，我们将 runoob.php 内容改为:</p>
<pre><code>&lt;?php
echo 'runoob';
?&gt;
</code></pre>
<p>创建 change_site 分支：</p>
<pre><code class="language-bash">$ git checkout -b change_site
Switched to a new branch 'change_site'
$ vim runoob.php
$ head -3 runoob.php
&lt;?php
echo 'runoob';
?&gt;
$ git commit -am 'changed the runoob.php'
[change_site 7774248] changed the runoob.php
 1 file changed, 3 insertions(+)
 
</code></pre>
<p>将修改的内容提交到 change_site 分支中。 现在，假如切换回 master 分支我们可以看内容恢复到我们修改前的(空文件，没有代码)，我们再次修改 runoob.php 文件。</p>
<pre><code class="language-bash">$ git checkout master
Switched to branch 'master'
$ cat runoob.php
$ vim runoob.php    # 修改内容如下
$ cat runoob.php
&lt;?php
echo 1;
?&gt;
$ git diff
diff --git a/runoob.php b/runoob.php
index e69de29..ac60739 100644
--- a/runoob.php
+++ b/runoob.php
@@ -0,0 +1,3 @@
+&lt;?php
+echo 1;
+?&gt;
$ git commit -am '修改代码'
[master c68142b] 修改代码
 1 file changed, 3 insertions(+)
</code></pre>
<p>现在这些改变已经记录到我的 &quot;master&quot; 分支了。接下来我们将 &quot;change_site&quot; 分支合并过来。</p>
<pre><code class="language-bash">$ git merge change_site
Auto-merging runoob.php
CONFLICT (content): Merge conflict in runoob.php
Automatic merge failed; fix conflicts and then commit the result.

$ cat runoob.php     # 打开文件，看到冲突内容
&lt;?php
&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
echo 1;
=======
echo 'runoob';
&gt;&gt;&gt;&gt;&gt;&gt;&gt; change_site
?&gt;
</code></pre>
<p>我们将前一个分支合并到 master 分支，一个合并冲突就出现了，接下来我们需要手动去修改它。</p>
<pre><code class="language-bash">$ vim runoob.php 
$ cat runoob.php
&lt;?php
echo 1;
echo 'runoob';
?&gt;
$ git diff
diff --cc runoob.php
index ac60739,b63d7d7..0000000
--- a/runoob.php
+++ b/runoob.php
@@@ -1,3 -1,3 +1,4 @@@
  &lt;?php
 +echo 1;
+ echo 'runoob';
  ?&gt;
</code></pre>
<p>在 Git 中，我们可以用 git add 要告诉 Git 文件冲突已经解决</p>
<pre><code class="language-bash">$ git status -s
UU runoob.php
$ git add runoob.php
$ git status -s
M  runoob.php
$ git commit
[master 88afe0e] Merge branch 'change_site'
</code></pre>
<p>现在我们成功解决了合并中的冲突，并提交了结果。</p>
<h1 id="git-查看提交历史">Git 查看提交历史</h1>
<p>Git 提交历史一般常用两个命令：</p>
<ul>
<li>
<p><strong>git log</strong> - 查看历史提交记录。</p>
</li>
<li>
<p><strong>git blame <file></strong> - 以列表形式查看指定文件的历史修改记录。</p>
</li>
<li>
<h3 id="git-log">git log</h3>
</li>
<li>
<p>在使用 Git 提交了若干更新之后，又或者克隆了某个项目，想回顾下提交历史，我们可以使用 <strong>git log</strong> 命令查看。</p>
</li>
<li>
<p>针对我们前一章节的操作，使用 <strong>git log</strong> 命令列出历史提交记录如下：</p>
</li>
<li>
<pre><code>$ git log
commit d5e9fc2c811e0ca2b2d28506ef7dc14171a207d9 (HEAD -&gt; master)
Merge: c68142b 7774248
Author: runoob &lt;test@runoob.com&gt;
Date:   Fri May 3 15:55:58 2019 +0800

    Merge branch 'change_site'

commit c68142b562c260c3071754623b08e2657b4c6d5b
Author: runoob &lt;test@runoob.com&gt;
Date:   Fri May 3 15:52:12 2019 +0800

    修改代码

commit 777424832e714cf65d3be79b50a4717aea51ab69 (change_site)
Author: runoob &lt;test@runoob.com&gt;
Date:   Fri May 3 15:49:26 2019 +0800

    changed the runoob.php

commit c1501a244676ff55e7cccac1ecac0e18cbf6cb00
Author: runoob &lt;test@runoob.com&gt;
Date:   Fri May 3 15:35:32 2019 +0800
</code></pre>
</li>
<li>
<p>我们可以用 --oneline 选项来查看历史记录的简洁的版本。</p>
</li>
<li>
<pre><code>$ git log --oneline
$ git log --oneline
d5e9fc2 (HEAD -&gt; master) Merge branch 'change_site'
c68142b 修改代码
7774248 (change_site) changed the runoob.php
c1501a2 removed test.txt、add runoob.php
3e92c19 add test.txt
3b58100 第一次版本提交
</code></pre>
</li>
<li>
<p>这告诉我们的是，此项目的开发历史。</p>
</li>
<li>
<p>我们还可以用 --graph 选项，查看历史中什么时候出现了分支、合并。以下为相同的命令，开启了拓扑图选项：</p>
</li>
<li>
<pre><code>*   d5e9fc2 (HEAD -&gt; master) Merge branch 'change_site'
|\  
| * 7774248 (change_site) changed the runoob.php
* | c68142b 修改代码
|/  
* c1501a2 removed test.txt、add runoob.php
* 3e92c19 add test.txt
* 3b58100 第一次版本提交
</code></pre>
</li>
<li>
<p>现在我们可以更清楚明了地看到何时工作分叉、又何时归并。</p>
</li>
<li>
<p>你也可以用 <strong>--reverse</strong> 参数来逆向显示所有日志。</p>
</li>
<li>
<pre><code>$ git log --reverse --oneline
3b58100 第一次版本提交
3e92c19 add test.txt
c1501a2 removed test.txt、add runoob.php
7774248 (change_site) changed the runoob.php
c68142b 修改代码
d5e9fc2 (HEAD -&gt; master) Merge branch 'change_site'
</code></pre>
</li>
<li>
<p>如果只想查找指定用户的提交日志可以使用命令：git log --author , 例如，比方说我们要找 Git 源码中 Linus 提交的部分：</p>
</li>
<li>
<pre><code>$ git log --author=Linus --oneline -5
81b50f3 Move 'builtin-*' into a 'builtin/' subdirectory
3bb7256 make &quot;index-pack&quot; a built-in
377d027 make &quot;git pack-redundant&quot; a built-in
b532581 make &quot;git unpack-file&quot; a built-in
112dd51 make &quot;mktag&quot; a built-in
</code></pre>
</li>
<li>
<p>如果你要指定日期，可以执行几个选项：--since 和 --before，但是你也可以用 --until 和 --after。</p>
</li>
<li>
<p>例如，如果我要看 Git 项目中三周前且在四月十八日之后的所有提交，我可以执行这个（我还用了 --no-merges 选项以隐藏合并提交）：</p>
</li>
<li>
<pre><code>$ git log --oneline --before={3.weeks.ago} --after={2010-04-18} --no-merges
5469e2d Git 1.7.1-rc2
d43427d Documentation/remote-helpers: Fix typos and improve language
272a36b Fixup: Second argument may be any arbitrary string
b6c8d2d Documentation/remote-helpers: Add invocation section
5ce4f4e Documentation/urls: Rewrite to accomodate transport::address
00b84e9 Documentation/remote-helpers: Rewrite description
03aa87e Documentation: Describe other situations where -z affects git diff
77bc694 rebase-interactive: silence warning when no commits rewritten
636db2c t3301: add tests to use --format=&quot;%N&quot;
</code></pre>
</li>
<li>
<p>更多 git log 命令可查看：http://git-scm.com/docs/git-log</p>
</li>
<li>
<h3 id="git-blame">git blame</h3>
</li>
<li>
<p>如果要查看指定文件的修改记录可以使用 git blame 命令，格式如下：</p>
</li>
<li>
<pre><code>git blame &lt;file&gt;
</code></pre>
</li>
<li>
<p>git blame 命令是以列表形式显示修改记录，如下实例：</p>
</li>
<li>
<pre><code>$ git blame README 
^d2097aa (tianqixin 2020-08-25 14:59:25 +0800 1) # Runoob Git 测试
db9315b0 (runoob    2020-08-25 16:00:23 +0800 2) # 菜鸟教程 
</code></pre>
</li>
</ul>
<h1 id="git-标签">Git 标签</h1>
<p>如果你达到一个重要的阶段，并希望永远记住那个特别的提交快照，你可以使用 git tag 给它打上标签。</p>
<p>比如说，我们想为我们的 runoob 项目发布一个&quot;1.0&quot;版本。 我们可以用 git tag -a v1.0 命令给最新一次提交打上（HEAD）&quot;v1.0&quot;的标签。</p>
<p>-a 选项意为&quot;创建一个带注解的标签&quot;。 不用 -a 选项也可以执行的，但它不会记录这标签是啥时候打的，谁打的，也不会让你添加个标签的注解。 我推荐一直创建带注解的标签。</p>
<pre><code class="language-bash">$ git tag -a v1.0 
</code></pre>
<p>当你执行 git tag -a 命令时，Git 会打开你的编辑器，让你写一句标签注解，就像你给提交写注解一样。</p>
<p>现在，注意当我们执行 git log --decorate 时，我们可以看到我们的标签了：</p>
<pre><code>*   d5e9fc2 (HEAD -&gt; master) Merge branch 'change_site'
|\  
| * 7774248 (change_site) changed the runoob.php
* | c68142b 修改代码
|/  
* c1501a2 removed test.txt、add runoob.php
* 3e92c19 add test.txt
* 3b58100 第一次版本提交
</code></pre>
<p>如果我们忘了给某个提交打标签，又将它发布了，我们可以给它追加标签。</p>
<p>例如，假设我们发布了提交 85fc7e7(上面实例最后一行)，但是那时候忘了给它打标签。 我们现在也可以：</p>
<pre><code class="language-bash">$ git tag -a v0.9 85fc7e7
$ git log --oneline --decorate --graph
*   d5e9fc2 (HEAD -&gt; master) Merge branch 'change_site'
|\  
| * 7774248 (change_site) changed the runoob.php
* | c68142b 修改代码
|/  
* c1501a2 removed test.txt、add runoob.php
* 3e92c19 add test.txt
* 3b58100 (tag: v0.9) 第一次版本提交
</code></pre>
<p>如果我们要查看所有标签可以使用以下命令：</p>
<pre><code class="language-bash">$ git tag
v0.9
v1.0
</code></pre>
<p>指定标签信息命令：</p>
<pre><code>git tag -a &lt;tagname&gt; -m &quot;runoob.com标签&quot;
</code></pre>
<p>PGP签名标签命令：</p>
<pre><code>git tag -s &lt;tagname&gt; -m &quot;runoob.com标签&quot;
</code></pre>
<p><a href="https://www.runoob.com/git/git-commit-history.html">Git 查看提交历史</a></p>
<p><a href="https://www.runoob.com/git/git-remote-repo.html">Git 远程仓库(Github)</a></p>
<h2 id="1-篇笔记-写笔记">1 篇笔记 写笔记</h2>
<ol>
<li></li>
</ol>
<pre><code> 宋某人c

sya***g@iroogoo.com
</code></pre>
<p>196</p>
<p>1、标签介绍</p>
<p>发布一个版本时，我们通常先在版本库中打一个标签（tag），这样就唯一确定了打标签时刻的版本。将来无论什么时候，取某个标签的版本，就是把那个打标签的时刻的历史版本取出来。</p>
<p>所以，标签也是版本库的一个快照。</p>
<p>Git 的标签虽然是版本库的快照，但其实它就是指向某个 commit 的指针（跟分支很像对不对？但是分支可以移动，标签不能移动），所以，创建和删除标签都是瞬间完成的。</p>
<blockquote>
<p>Git有commit，为什么还要引入tag？</p>
<p>&quot;请把上周一的那个版本打包发布，commit号是6a5819e…&quot;</p>
<p>&quot;一串乱七八糟的数字不好找！&quot;</p>
<p>如果换一个办法：</p>
<p>&quot;请把上周一的那个版本打包发布，版本号是v1.2&quot;</p>
<p>&quot;好的，按照tag v1.2查找commit就行！&quot;</p>
<p>所以，tag就是一个让人容易记住的有意义的名字，它跟某个commit绑在一起。</p>
</blockquote>
<p>同大多数 VCS 一样，Git 也可以对某一时间点上的版本打上标签。人们在发布某个软件版本（比如 v1.0 等等）的时候，经常这么做。</p>
<p>本节我们一起来学习如何列出所有可用的标签，如何新建标签，以及各种不同类型标签之间的差别。</p>
<hr>
<h2 id="新建标签">新建标签</h2>
<p>Git 使用的标签有两种类型：<strong>轻量级的（lightweight）<strong>和</strong>含附注的（annotated）</strong>。</p>
<p>轻量级标签就像是个不会变化的分支，实际上它就是个指向特定提交对象的引用。</p>
<p>而含附注标签，实际上是存储在仓库中的一个独立对象，它有自身的校验和信息，包含着标签的名字，电子邮件地址和日期，以及标签说明，标签本身也允许使用 GNU Privacy Guard (GPG) 来签署或验证。</p>
<p>一般我们都建议使用含附注型的标签，以便保留相关信息；</p>
<p>当然，如果只是临时性加注标签，或者不需要旁注额外信息，用轻量级标签也没问题。</p>
<h3 id="2-创建标签">2 创建标签</h3>
<pre><code>[root@Git git]# git tag v1.0
</code></pre>
<h3 id="3-查看已有标签">3 查看已有标签</h3>
<pre><code>[root@Git git]# git tag
v1.0
[root@Git git]# git tag v1.1
[root@Git git]# git tag
v1.0
v1.1
</code></pre>
<h3 id="4-删除标签">4 删除标签</h3>
<pre><code>[root@Git git]# git tag -d v1.1
Deleted tag ‘v1.1’ (was 91388f0)
[root@Git git]# git tag
v1.0
</code></pre>
<h3 id="5-查看此版本所修改的内容">5 查看此版本所修改的内容</h3>
<pre><code>[root@Git git]# git show v1.0
commit 91388f0883903ac9014e006611944f6688170ef4
Author: &quot;syaving&quot; &lt;&quot;819044347@qq.com&quot;&gt;
Date: Fri Dec 16 02:32:05 2016 +0800
commit dir
diff –git a/readme b/readme
index 7a3d711..bfecb47 100644
— a/readme
+++ b/readme
@@ -1,2 +1,3 @@
text
hello git
+use commit
[root@Git git]# git log –oneline
91388f0 commit dir
e435fe8 add readme
2525062 add readme
</code></pre>
<h1 id="git-远程仓库github">Git 远程仓库(Github)</h1>
<p>Git 并不像 SVN 那样有个中心服务器。</p>
<p>目前我们使用到的 Git 命令都是在本地执行，如果你想通过 Git 分享你的代码或者与其他开发人员合作。 你就需要将数据放到一台其他开发人员能够连接的服务器上。</p>
<p>本例使用了 Github 作为远程仓库，你可以先阅读我们的 <a href="https://www.runoob.com/w3cnote/git-guide.html">Github 简明教程。</a></p>
<figure data-type="image" tabindex="4"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/Git-push-command.jpeg" alt="img" loading="lazy"></figure>
<hr>
<h2 id="添加远程库">添加远程库</h2>
<p>要添加一个新的远程仓库，可以指定一个简单的名字，以便将来引用,命令格式如下：</p>
<pre><code>git remote add [shortname] [url]
</code></pre>
<p>本例以 Github 为例作为远程仓库，如果你没有 Github 可以在官网 https://github.com/注册。</p>
<p>由于你的本地 Git 仓库和 GitHub 仓库之间的传输是通过SSH加密的，所以我们需要配置验证信息：</p>
<p>使用以下命令生成 SSH Key：</p>
<pre><code class="language-bash">$ ssh-keygen -t rsa -C &quot;youremail@example.com&quot;
</code></pre>
<p>后面的 <strong>your_email@youremail.com</strong> 改为你在 Github 上注册的邮箱，之后会要求确认路径和输入密码，我们这使用默认的一路回车就行。</p>
<p>成功的话会在 <strong>~/</strong> 下生成 <strong>.ssh</strong> 文件夹，进去，打开 <strong>id_rsa.pub</strong>，复制里面的 <strong>key</strong>。</p>
<pre><code class="language-bash">$ ssh-keygen -t rsa -C &quot;429240967@qq.com&quot;
Generating public/private rsa key pair.
Enter file in which to save the key (/Users/tianqixin/.ssh/id_rsa): 
Enter passphrase (empty for no passphrase):    # 直接回车
Enter same passphrase again:                   # 直接回车
Your identification has been saved in /Users/tianqixin/.ssh/id_rsa.
Your public key has been saved in /Users/tianqixin/.ssh/id_rsa.pub.
The key fingerprint is:
SHA256:MDKVidPTDXIQoJwoqUmI4LBAsg5XByBlrOEzkxrwARI 429240967@qq.com
The key's randomart image is:
+---[RSA 3072]----+
|E*+.+=**oo       |
|%Oo+oo=o. .      |
|%**.o.o.         |
|OO.  o o         |
|+o+     S        |
|.                |
|                 |
|                 |
|                 |
+----[SHA256]-----+
</code></pre>
<p>回到 github 上，进入 Account =&gt; Settings（账户配置）。</p>
<figure data-type="image" tabindex="5"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/48840BF0-992F-4CCC-A388-15CB74819D88.jpg" alt="img" loading="lazy"></figure>
<p>左边选择 <strong>SSH and GPG keys</strong>，然后点击 <strong>New SSH key</strong> 按钮,title 设置标题，可以随便填，粘贴在你电脑上生成的 key。</p>
<figure data-type="image" tabindex="6"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/B0589847-A498-4415-8700-252BDE1B20C0.jpg" alt="img" loading="lazy"></figure>
<figure data-type="image" tabindex="7"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/106AD534-A38A-47F3-88A3-B7BE3F2FEEF1.jpg" alt="img" loading="lazy"></figure>
<p>添加成功后界面如下所示</p>
<figure data-type="image" tabindex="8"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/EC8F8872-091A-4CAB-90F2-616F34F350A9.jpg" alt="img" loading="lazy"></figure>
<p>为了验证是否成功，输入以下命令：</p>
<pre><code class="language-bash">$ ssh -T git@github.com
The authenticity of host 'github.com (52.74.223.119)' can't be established.
RSA key fingerprint is SHA256:nThbg6kXUpJWGl7E1IGOCspRomTxdCARLviKw6E5SY8.
Are you sure you want to continue connecting (yes/no/[fingerprint])? yes                   # 输入 yes
Warning: Permanently added 'github.com,52.74.223.119' (RSA) to the list of known hosts.
Hi tianqixin! You've successfully authenticated, but GitHub does not provide shell access. # 成功信息
</code></pre>
<p>以下命令说明我们已成功连上 Github。</p>
<p>之后登录后点击&quot; New repository &quot; 如下图所示：</p>
<figure data-type="image" tabindex="9"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/github1.jpg" alt="img" loading="lazy"></figure>
<p>之后在在Repository name 填入 runoob-git-test(远程仓库名) ，其他保持默认设置，点击&quot;Create repository&quot;按钮，就成功地创建了一个新的Git仓库：</p>
<figure data-type="image" tabindex="10"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/299CF000-7B6E-4BEC-B8C2-D9AEB053307B.jpg" alt="img" loading="lazy"></figure>
<p>创建成功后，显示如下信息：</p>
<figure data-type="image" tabindex="11"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/1BCB4379-1A25-4C77-BB82-92B3E7185435.jpg" alt="img" loading="lazy"></figure>
<p>以上信息告诉我们可以从这个仓库克隆出新的仓库，也可以把本地仓库的内容推送到GitHub仓库。</p>
<p>现在，我们根据 GitHub 的提示，在本地的仓库下运行命令：</p>
<pre><code class="language-bash">$ mkdir runoob-git-test                     # 创建测试目录
$ cd runoob-git-test/                       # 进入测试目录
$ echo &quot;# 菜鸟教程 Git 测试&quot; &gt;&gt; README.md     # 创建 README.md 文件并写入内容
$ ls                                        # 查看目录下的文件
README
$ git init                                  # 初始化
$ git add README.md                         # 添加文件
$ git commit -m &quot;添加 README.md 文件&quot;        # 提交并备注信息
[master (root-commit) 0205aab] 添加 README.md 文件
 1 file changed, 1 insertion(+)
 create mode 100644 README.md

# 提交到 Github
$ git remote add origin git@github.com:tianqixin/runoob-git-test.git
$ git push -u origin master
</code></pre>
<p>以下命令请根据你在Github成功创建新仓库的地方复制，而不是根据我提供的命令，因为我们的Github用户名不一样，仓库名也不一样。</p>
<p>接下来我们返回 Github 创建的仓库，就可以看到文件已上传到 Github上：</p>
<figure data-type="image" tabindex="12"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/53CA927D-F36F-4A00-AFB2-5EAED05B535E.jpg" alt="img" loading="lazy"></figure>
<hr>
<h2 id="查看当前的远程库">查看当前的远程库</h2>
<p>要查看当前配置有哪些远程仓库，可以用命令：</p>
<pre><code>git remote
</code></pre>
<h3 id="实例">实例</h3>
<pre><code class="language-bash">$ git remote
origin
$ git remote -v
origin    git@github.com:tianqixin/runoob-git-test.git (fetch)
origin    git@github.com:tianqixin/runoob-git-test.git (push)
</code></pre>
<p>执行时加上 -v 参数，你还可以看到每个别名的实际链接地址。</p>
<hr>
<h2 id="提取远程仓库">提取远程仓库</h2>
<p>Git 有两个命令用来提取远程仓库的更新。</p>
<p>1、从远程仓库下载新分支与数据：</p>
<pre><code>git fetch
</code></pre>
<p>该命令执行完后需要执行 git merge 远程分支到你所在的分支。</p>
<p>2、从远端仓库提取数据并尝试合并到当前分支：</p>
<pre><code>git merge
</code></pre>
<p>该命令就是在执行 <strong>git fetch</strong> 之后紧接着执行 <strong>git merge</strong> 远程分支到你所在的任意分支。</p>
<figure data-type="image" tabindex="13"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/main-qimg-00a6b5a8ec82400657444504c4d4d1a7.png" alt="img" loading="lazy"></figure>
<p>假设你配置好了一个远程仓库，并且你想要提取更新的数据，你可以首先执行 <strong>git fetch [alias]</strong> 告诉 Git 去获取它有你没有的数据，然后你可以执行 <strong>git merge [alias]/[branch]</strong> 以将服务器上的任何更新（假设有人这时候推送到服务器了）合并到你的当前分支。</p>
<p>接下来我们在 Github 上点击&quot; README.md&quot; 并在线修改它:</p>
<figure data-type="image" tabindex="14"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/C5A6670F-202D-4F2C-8A63-07CEA37BB67A.jpg" alt="img" loading="lazy"></figure>
<p>然后我们在本地更新修改。</p>
<pre><code class="language-bash">$ git fetch origin
remote: Counting objects: 3, done.
remote: Compressing objects: 100% (2/2), done.
remote: Total 3 (delta 0), reused 0 (delta 0), pack-reused 0
Unpacking objects: 100% (3/3), done.
From github.com:tianqixin/runoob-git-test
   0205aab..febd8ed  master     -&gt; origin/master
</code></pre>
<p>以上信息&quot;0205aab..febd8ed master -&gt; origin/master&quot; 说明 master 分支已被更新，我们可以使用以下命令将更新同步到本地：</p>
<pre><code class="language-bash">$ git merge origin/master
Updating 0205aab..febd8ed
Fast-forward
 README.md | 1 +
 1 file changed, 1 insertion(+)
</code></pre>
<p>查看 README.md 文件内容：</p>
<pre><code class="language-bash">$ cat README.md 
# 菜鸟教程 Git 测试
## 第一次修改内容
</code></pre>
<hr>
<h2 id="推送到远程仓库">推送到远程仓库</h2>
<p>推送你的新分支与数据到某个远端仓库命令:</p>
<pre><code>git push [alias] [branch]
</code></pre>
<p>以上命令将你的 [branch] 分支推送成为 [alias] 远程仓库上的 [branch] 分支，实例如下。</p>
<pre><code class="language-bash">$ touch runoob-test.txt      # 添加文件
$ git add runoob-test.txt 
$ git commit -m &quot;添加到远程&quot;
master 69e702d] 添加到远程
 1 file changed, 0 insertions(+), 0 deletions(-)
 create mode 100644 runoob-test.txt

$ git push origin master    # 推送到 Github
</code></pre>
<p>重新回到我们的 Github 仓库，可以看到文件已经提交上来了：</p>
<figure data-type="image" tabindex="15"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/79A84530-7DC0-4D25-9F83-8776433A4C32.jpg" alt="img" loading="lazy"></figure>
<hr>
<h2 id="删除远程仓库">删除远程仓库</h2>
<p>删除远程仓库你可以使用命令：</p>
<pre><code>git remote rm [别名]
</code></pre>
<h3 id="实例-2">实例</h3>
<pre><code class="language-bash">$ git remote -v
origin    git@github.com:tianqixin/runoob-git-test.git (fetch)
origin    git@github.com:tianqixin/runoob-git-test.git (push)

# 添加仓库 origin2
$ git remote add origin2 git@github.com:tianqixin/runoob-git-test.git

$ git remote -v
origin    git@github.com:tianqixin/runoob-git-test.git (fetch)
origin    git@github.com:tianqixin/runoob-git-test.git (push)
origin2    git@github.com:tianqixin/runoob-git-test.git (fetch)
origin2    git@github.com:tianqixin/runoob-git-test.git (push)

# 删除仓库 origin2
$ git remote rm origin2
$ git remote -v
origin    git@github.com:tianqixin/runoob-git-test.git (fetch)
origin    git@github.com:tianqixin/runoob-git-test.git (push)
</code></pre>
<h1 id="git-服务器搭建">Git 服务器搭建</h1>
<p>上一章节中我们远程仓库使用了 Github，Github 公开的项目是免费的，2019 年开始 Github 私有存储库也可以无限制使用。</p>
<p>这当然我们也可以自己搭建一台 Git 服务器作为私有仓库使用。</p>
<p>接下来我们将以 Centos 为例搭建 Git 服务器。</p>
<h3 id="1-安装git">1、安装Git</h3>
<pre><code class="language-bash">$ yum install curl-devel expat-devel gettext-devel openssl-devel zlib-devel perl-devel
$ yum install git
</code></pre>
<p>接下来我们 创建一个git用户组和用户，用来运行git服务：</p>
<pre><code class="language-bash">$ groupadd git
$ useradd git -g git
</code></pre>
<h3 id="2-创建证书登录">2、创建证书登录</h3>
<p>收集所有需要登录的用户的公钥，公钥位于id_rsa.pub文件中，把我们的公钥导入到/home/git/.ssh/authorized_keys文件里，一行一个。</p>
<p>如果没有该文件创建它：</p>
<pre><code class="language-bash">$ cd /home/git/
$ mkdir .ssh
$ chmod 755 .ssh
$ touch .ssh/authorized_keys
$ chmod 644 .ssh/authorized_keys
</code></pre>
<h3 id="3-初始化git仓库">3、初始化Git仓库</h3>
<p>首先我们选定一个目录作为Git仓库，假定是/home/gitrepo/runoob.git，在/home/gitrepo目录下输入命令：</p>
<pre><code class="language-bash">$ cd /home
$ mkdir gitrepo
$ chown git:git gitrepo/
$ cd gitrepo

$ git init --bare runoob.git
Initialized empty Git repository in /home/gitrepo/runoob.git/
</code></pre>
<p>以上命令Git创建一个空仓库，服务器上的Git仓库通常都以.git结尾。然后，把仓库所属用户改为git：</p>
<pre><code class="language-bash">$ chown -R git:git runoob.git
</code></pre>
<h3 id="4-克隆仓库">4、克隆仓库</h3>
<pre><code class="language-bash">$ git clone git@192.168.45.4:/home/gitrepo/runoob.git
Cloning into 'runoob'...
warning: You appear to have cloned an empty repository.
Checking connectivity... done.
</code></pre>
<p>192.168.45.4 为 Git 所在服务器 ip ，你需要将其修改为你自己的 Git 服务 ip。</p>
<p>这样我们的 Git 服务器安装就完成。</p>
<h1 id="sourcetree-使用教程">Sourcetree 使用教程</h1>
<figure data-type="image" tabindex="16"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/Sourcetree@2x-icon-blue.png" alt="img" loading="lazy"></figure>
<p>Git 有很多图形界面工具 ( GUI )，比如 SourceTree、Github Desktop、TortoiseGit 等。</p>
<p>SourceTree 是一个 Git 客户端管理工具，适用于 Windows 和 Mac 系统。</p>
<p>SourceTree 简化了开发者与代码仓库之间的 Git 操作方式，我们可以通过界面菜单很方便的处理 Git 操作，而不需要通过命令。</p>
<p>通过 SourceTree，我们可以管理所有的 Git 库，无论是远程还是本地的。SourceTree 支持 Bitbucket、GitHub 以及 Gitlab 等远程仓库。</p>
<hr>
<h2 id="sourcetree-安装">Sourcetree 安装</h2>
<p>我们可以在 Sourcetree 官网 https://www.sourcetreeapp.com/ 直接点击下载按钮来下载：</p>
<figure data-type="image" tabindex="17"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-1.jpeg" alt="img" loading="lazy"></figure>
<p>其他版本可以查看：https://www.sourcetreeapp.com/download-archives</p>
<p>下载完成后，解压 ZIP 文件，双击应用程序文件（Windows 为 EXE 或 Mac 为 DMG）进行安装。</p>
<p>安装过程中，我们只需要同意 Atlassian 客户协议并点击 Continue 即可。</p>
<figure data-type="image" tabindex="18"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree_aa.png" alt="img" loading="lazy"></figure>
<p>创建 Bitbucket 账户可跳过初始设置，加载 SSH 密钥，如果有会默认加载进来，也可以后面再设置。</p>
<figure data-type="image" tabindex="19"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree_account.png" alt="img" loading="lazy"></figure>
<p>按照提示操作，直到您完成设置。如果您还没有准备好克隆存储库，请单击 Skip Setup。</p>
<hr>
<h2 id="连接-github-账户">连接 Github 账户</h2>
<p>如果你还没有 Github 账户需要创建一个，可以参考 <a href="https://www.runoob.com/git/git-remote-repo.html">Git 远程仓库(Github)</a>。</p>
<p>接下来我们将介绍使用 Sourcetree 管理我们的 Github 账户。</p>
<p>我们可以将多个帐户添加到 Sourcetree。</p>
<p>单击右上角的按钮并选择<strong>帐户</strong>。</p>
<figure data-type="image" tabindex="20"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-2.jpeg" alt="img" loading="lazy"></figure>
<p>接下来我们通过添加账户按钮添加 Github 账户。</p>
<figure data-type="image" tabindex="21"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-3.jpeg" alt="img" loading="lazy"></figure>
<p>Github 账户添加完成后，就可以直接克隆我们自己的账户：</p>
<figure data-type="image" tabindex="22"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-4.jpeg" alt="img" loading="lazy"></figure>
<p>也可以克隆其他远程仓库：</p>
<figure data-type="image" tabindex="23"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-5.jpeg" alt="img" loading="lazy"></figure>
<p>克隆完成后，我们就可以进行提交、拉取、合并等操作：</p>
<figure data-type="image" tabindex="24"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-6.jpeg" alt="img" loading="lazy"></figure>
<hr>
<h2 id="创建本地仓库">创建本地仓库</h2>
<p>单击<strong>新建</strong>，然后选择<strong>创建本地仓库</strong>。</p>
<figure data-type="image" tabindex="25"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-7.jpeg" alt="img" loading="lazy"></figure>
<p>输入您的目标路径和存储库名称，然后点击<strong>创建</strong>。</p>
<figure data-type="image" tabindex="26"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-8.jpeg" alt="img" loading="lazy"></figure>
<p>创建完成后就会显示在本地的列表中：</p>
<figure data-type="image" tabindex="27"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-9.jpeg" alt="img" loading="lazy"></figure>
<p>如果我们本地已经存在一个仓库，可以直接添加它。</p>
<p>点击<strong>新建</strong>，然后 点击<strong>添加已经存在的本地仓库</strong>：</p>
<figure data-type="image" tabindex="28"><img src="https://euyy.github.io/post-images/git-shi-yong-jiao-cheng.assets/sourcetree-10.jpeg" alt="img" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Leetcode每日一题(2022/12/29)]]></title>
        <id>https://euyy.github.io/post/leetcode-mei-ri-yi-ti/</id>
        <link href="https://euyy.github.io/post/leetcode-mei-ri-yi-ti/">
        </link>
        <updated>2022-12-29T03:47:32.000Z</updated>
        <content type="html"><![CDATA[<h1 id="2032至少在两个数组中出现的值"><a href="https://leetcode.cn/problems/two-out-of-three/description/">2032.至少在两个数组中出现的值</a></h1>
<p>给你三个整数数组 <code>nums1</code>、<code>nums2</code> 和 <code>nums3</code> ，请你构造并返回一个 <strong>元素各不相同的</strong> 数组，且由 <strong>至少</strong> 在 <strong>两个</strong> 数组中出现的所有值组成。数组中的元素可以按 <strong>任意</strong> 顺序排列。</p>
<p><strong>示例 1：</strong></p>
<pre><code>输入：nums1 = [1,1,3,2], nums2 = [2,3], nums3 = [3]
输出：[3,2]
解释：至少在两个数组中出现的所有值为：
- 3 ，在全部三个数组中都出现过。
- 2 ，在数组 nums1 和 nums2 中出现过。
</code></pre>
<p><strong>示例 2：</strong></p>
<pre><code>输入：nums1 = [3,1], nums2 = [2,3], nums3 = [1,2]
输出：[2,3,1]
解释：至少在两个数组中出现的所有值为：
- 2 ，在数组 nums2 和 nums3 中出现过。
- 3 ，在数组 nums1 和 nums2 中出现过。
- 1 ，在数组 nums1 和 nums3 中出现过。
</code></pre>
<p><strong>示例 3：</strong></p>
<pre><code>输入：nums1 = [1,2,2], nums2 = [4,3,3], nums3 = [5]
输出：[]
解释：不存在至少在两个数组中出现的值。
</code></pre>
<p><strong>提示：</strong></p>
<ul>
<li><code>1 &lt;= nums1.length, nums2.length, nums3.length &lt;= 100</code></li>
<li><code>1 &lt;= nums1[i], nums2[j], nums3[k] &lt;= 100</code></li>
</ul>
<h2 id="思路点拨">思路点拨</h2>
<h3 id="方法一哈希表位存储">方法一：哈希表+位存储</h3>
<p>统计不同元素在不同数组中出现的次数即可。</p>
<p>需要注意的是，一个元素可能在一个数组中出现多次。</p>
<p>所以可以使用哈希表来存储一个元素在几个数组中出现过，利用位运算来表示一个元素在第几个数组中出现过。也就是说，把数组 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>n</mi><mi>u</mi><mi>m</mi></mrow><annotation encoding="application/x-tex">num</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.43056em;vertical-align:0em;"></span><span class="mord mathdefault">n</span><span class="mord mathdefault">u</span><span class="mord mathdefault">m</span></span></span></span> 作为哈希表的 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>k</mi><mi>e</mi><mi>y</mi></mrow><annotation encoding="application/x-tex">key</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8888799999999999em;vertical-align:-0.19444em;"></span><span class="mord mathdefault" style="margin-right:0.03148em;">k</span><span class="mord mathdefault">e</span><span class="mord mathdefault" style="margin-right:0.03588em;">y</span></span></span></span>， 把在哪些数组中出现过作为 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>v</mi><mi>a</mi><mi>l</mi><mi>u</mi><mi>e</mi></mrow><annotation encoding="application/x-tex">value</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.69444em;vertical-align:0em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">v</span><span class="mord mathdefault">a</span><span class="mord mathdefault" style="margin-right:0.01968em;">l</span><span class="mord mathdefault">u</span><span class="mord mathdefault">e</span></span></span></span>，如果在第一个数组中出现过，则把第 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>i</mi></mrow><annotation encoding="application/x-tex">i</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.65952em;vertical-align:0em;"></span><span class="mord mathdefault">i</span></span></span></span> 个二进制位置为 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1</mn></mrow><annotation encoding="application/x-tex">1</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span> 。这样就可以通过判断一个数的 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>v</mi><mi>a</mi><mi>l</mi><mi>u</mi><mi>e</mi></mrow><annotation encoding="application/x-tex">value</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.69444em;vertical-align:0em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">v</span><span class="mord mathdefault">a</span><span class="mord mathdefault" style="margin-right:0.01968em;">l</span><span class="mord mathdefault">u</span><span class="mord mathdefault">e</span></span></span></span> ，来判断是否在多个数组中出现过。如果 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>v</mi><mi>a</mi><mi>l</mi><mi>u</mi><mi>e</mi></mrow><annotation encoding="application/x-tex">value</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.69444em;vertical-align:0em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">v</span><span class="mord mathdefault">a</span><span class="mord mathdefault" style="margin-right:0.01968em;">l</span><span class="mord mathdefault">u</span><span class="mord mathdefault">e</span></span></span></span> 有多余一个二进制 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1</mn></mrow><annotation encoding="application/x-tex">1</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span>，则表示有多个数组出现过。</p>
<p>这里，判断一个数中有几个二进制 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1</mn></mrow><annotation encoding="application/x-tex">1</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span> 可以使用位运算 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>n</mi><mi>u</mi><mi>m</mi><mi mathvariant="normal">&amp;</mi><mo>(</mo><mi>n</mi><mi>u</mi><mi>m</mi><mo>−</mo><mn>1</mn><mo>)</mo></mrow><annotation encoding="application/x-tex">num \&amp; (num - 1)</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathdefault">n</span><span class="mord mathdefault">u</span><span class="mord mathdefault">m</span><span class="mord">&amp;</span><span class="mopen">(</span><span class="mord mathdefault">n</span><span class="mord mathdefault">u</span><span class="mord mathdefault">m</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord">1</span><span class="mclose">)</span></span></span></span>  ，这个运算可以去除掉 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>n</mi><mi>u</mi><mi>m</mi></mrow><annotation encoding="application/x-tex">num</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.43056em;vertical-align:0em;"></span><span class="mord mathdefault">n</span><span class="mord mathdefault">u</span><span class="mord mathdefault">m</span></span></span></span> 中最后一个二进制 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1</mn></mrow><annotation encoding="application/x-tex">1</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span> .</p>
<h2 id="算法实现">算法实现</h2>
<pre><code class="language-c++">class Solution {
public:
    vector&lt;int&gt; twoOutOfThree(vector&lt;int&gt;&amp; nums1, vector&lt;int&gt;&amp; nums2, vector&lt;int&gt;&amp; nums3) {
		unordered_map&lt;int,int&gt; ump; // 定义存储数字以及出现在哪些数组的哈希结构
        for(int n : nums1)
        {
            ump[n] = 1; // 对于第一个数组中的数字，凡是出现过，则把值置为1
        }
        for(int n : nums2)
        {
            ump[n] |= 2; // 对于第二个数组中的数字，凡是出现过，则把第二位二进制置为1
        }
        for(int n : nums3)
        {
            ump[n] |= 4; // 对于第二个数组中的数字，凡是出现过，则把第三位二进制置为1
        }
        vector&lt;int&gt; ret; // 定义存储结果的数组
        for(auto &amp;[k,v] : ump) // 对于哈希表中的每个key-value对，进行判断
        {
            if(v &amp; (v-1)) // 如果v不止有一个二进制1，就把k添加进ret中
            {
                ret.push_back(k);
            }
        }
        return ret;
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Diffusion Model]]></title>
        <id>https://euyy.github.io/post/diffusion-model/</id>
        <link href="https://euyy.github.io/post/diffusion-model/">
        </link>
        <updated>2022-12-29T01:55:00.000Z</updated>
        <content type="html"><![CDATA[<p>第一篇</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Hello Gridea]]></title>
        <id>https://euyy.github.io/post/hello-gridea/</id>
        <link href="https://euyy.github.io/post/hello-gridea/">
        </link>
        <updated>2018-12-11T16:00:00.000Z</updated>
        <summary type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
]]></summary>
        <content type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
<!-- more -->
<p><a href="https://github.com/getgridea/gridea">Github</a><br>
<a href="https://gridea.dev/">Gridea 主页</a><br>
<a href="https://fehey.com/">示例网站</a></p>
<h2 id="特性">特性👇</h2>
<p>📝  你可以使用最酷的 <strong>Markdown</strong> 语法，进行快速创作</p>
<p>🌉  你可以给文章配上精美的封面图和在文章任意位置插入图片</p>
<p>🏷️  你可以对文章进行标签分组</p>
<p>📋  你可以自定义菜单，甚至可以创建外部链接菜单</p>
<p>💻  你可以在 <strong>Windows</strong>，<strong>MacOS</strong> 或 <strong>Linux</strong> 设备上使用此客户端</p>
<p>🌎  你可以使用 <strong>𝖦𝗂𝗍𝗁𝗎𝖻 𝖯𝖺𝗀𝖾𝗌</strong> 或 <strong>Coding Pages</strong> 向世界展示，未来将支持更多平台</p>
<p>💬  你可以进行简单的配置，接入 <a href="https://github.com/gitalk/gitalk">Gitalk</a> 或 <a href="https://github.com/SukkaW/DisqusJS">DisqusJS</a> 评论系统</p>
<p>🇬🇧  你可以使用<strong>中文简体</strong>或<strong>英语</strong></p>
<p>🌁  你可以任意使用应用内默认主题或任意第三方主题，强大的主题自定义能力</p>
<p>🖥  你可以自定义源文件夹，利用 OneDrive、百度网盘、iCloud、Dropbox 等进行多设备同步</p>
<p>🌱 当然 <strong>Gridea</strong> 还很年轻，有很多不足，但请相信，它会不停向前 🏃</p>
<p>未来，它一定会成为你离不开的伙伴</p>
<p>尽情发挥你的才华吧！</p>
<p>😘 Enjoy~</p>
]]></content>
    </entry>
</feed>